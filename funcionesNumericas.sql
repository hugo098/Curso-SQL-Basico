--=============================================================--
SELECT round(45.926, 2),
       round(45.923, 0),
       round(45.923, - 1)
FROM dual;

--=============================================================--

SELECT trunc(45.926, 2),
       trunc(45.923, 0),
       trunc(45.923, - 1)
FROM dual;

--=============================================================--

SELECT last_name,
       salary,
       mod(salary, 500)
FROM employees;